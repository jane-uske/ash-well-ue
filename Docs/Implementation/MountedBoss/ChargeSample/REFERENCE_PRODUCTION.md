# 原片复刻制作轮（2026-09-12）

## 已授权目标

在原工程、`codex/mounted-charge-standard` 工作分支上继续；初始基线 `bd791aa`，本轮制作前候选回退提交 `db1ee62`。本轮用户明确允许按原片重排冲锋时序、替换为真实起伏地形并重新验证碰撞；旧文档的“保留 3.55 秒时序、不改地形”不再适用。六招、守井者、正式按键和现有战斗规则保留，只精修冲锋，不扩故事等系统。

原片主参考：`Saved/MountedReference/BV1cw411M7xw-video.m4s` 的 19.3–25.1 秒，16–28 秒为上下文。对照使用原速与单一时间偏移，禁止通过变速或插帧掩盖差异。遮挡、出画和不能辨认的接触均标记不确定。

## Meshy 预算与资产

- 授权总上限 964 积分，首批上限 200；当前账户快照 2,364=964 月度+1,400 永久，下次续费页面显示 2026-10-09 22:47。永久额度保留，不新增购买；每次付费任务前检查余额并预留在途任务。
- 首批三件：遗迹墙段、前景扁岩、沿坡特征立杆。先独立参考图，再 Meshy 7 Ultra / 4K PBR 模型，Blender 修整，UE 材质/比例/碰撞/性能检查。模型生成预计每件 35 积分；实际以任务扣费记录为准。其余预算只用于看得到的必要缺口。
- 原始输出统一保存在 `meshy_output/` 独立目录，带任务 ID、预览、模型和贴图。修整派生文件另存 `SourceAssets/MountedReferenceProduction/`。现有骑士/马/斧盾原件不得覆盖。
- 草木必须具备适用的实时渲染结构、LOD、实例化和风动；不得直接用整块高面数树冠充当完成品。

## 阶段与验收

1. 准备：保存并推送工作分支及 LFS；核查录制时间戳、音轨与封装；检查启动/退出，保留崩溃报告。
2. A：按参考统一地形、镜头、人物体量和光照；修盾尺寸/朝向/挂点、握柄和坐姿；提供 UE 环绕及移动录像。
3. B：准备、加速、挥击、掠过、制动、恢复连续完成。保留独立人马骨架；AnimBP/混合/Montage/Notify/Notify State 与必要接触约束实际运行；补马匹制动/转向过渡。动作数据包含马 Montage、位移曲线和锁向点，以统一动作进度驱动。默认单一根位移与 Sweep，必要时只在此招验证 Root Motion/Motion Warping。
4. 地面查询覆盖地面随动、落地、中断、盾地接触与重试；替换固定 Home.Z 假设。改动画/尺寸/挂点/地形后，重测所有受影响判定。
5. C：自然 AI、完整血量、普通按键，真人初测 10–15 分钟；录像与日志同时记录，初测与最终验收独立。适配新骑士死亡姿态。

## 当前执行状态

最新（17:00）：原生动画、三件首批构件及一件必要远景候选已接入，累计140积分。16项六招功能回归通过（20260912-165133-3ea6961a）；坡地矩阵正在执行。B-SoleContact支持期滑动31.45cm/s，尚未达到视觉验收。死亡录像显示马匹未正确倒地，新死亡候选明确失败，正在检查重定向根部传递。A/B待验收，C待真人。以下早期条目保留为历史，不覆盖本段结论。

- 旧候选 `db1ee62` 已推送，30 个 LFS 对象 / 441 MB 上传完成，不合并 main。
- 三件 Meshy 模型及 4K PBR 已下载，共消耗 105，余额 2259；详见 `meshy-reference-budget.json`。派生尺寸墙段 400×75.9×200.1 cm、扁岩 330×233.5×64.8 cm、立杆高 400 cm。Blender 离线检查完成，正在接入 UE；新动作未交付。
- 地面查询/随动、盾地高度和中断落地已局部改造；布料改为最终模型/动画配置后再注册。构建成功，平地样本 hit_charge / sample_cleanup / retry 3 例通过，报告 `Saved/MountedBoss/runs/20260912-143414-582c9401/`。这不是新地形或最终版本通过。
- 原始 B 核查再次确认音频时长缺口，复核 `Saved/MountedReferenceProduction/original-B-audit.json`；原始音轨不变速、不填充。一次启动预检正常结束、未产生崩溃，不认定旧偶发崩溃已完全修复。
- 草木复用 Poly Haven CC0 源模型：grass_medium_01 与 jacaranda_tree，下载/许可来源见 SourceAssets/MountedReferenceProduction/Vegetation/download-manifest.json。Blender 树木减面曾在旧属性转 BMesh 时崩溃，保存 crash 文本；验证并清理属性后正常导出，保留 UV 与材质分配。
- 旧 A/B 是待验收候选；C 仍未完成，不因工具输入受限认定游戏失败。
- 布料首次渲染偶发 `GPUSkinVertexFactory.cpp:1343 bPrevious` 根因未确立，旧报告保留。一次成功启动不视为已修复。

## 交付约束

必须交付版本对应的启动入口、正常速度对照、未剪辑原片、日志、已知问题与回退方式；实测 UE 帧时间与采集帧率分开。目标本机 1920×1080、60 FPS，不从文件帧率或编译结果推断。功能验证覆盖上下坡/横坡/岩缘、命中/闪避/反打/取消/死亡/重试以及六招和守井者回归。工具不能制作或验证必要 UE 资产时明确报告，不用手写占位代替完成。

回退使用原工程的已提交版本进行受控恢复；先正常退出本工程并保存当前工作，再按本轮改动清单恢复 `db1ee62` 对应文件并重建。`bd791aa` 与旧录像继续保留，禁止全仓库强制 reset 覆盖并发工作。

## 2026-09-12 执行续记（动态验收未通过）

- 新三件模型与原生 HISM 植被已接入。首段 A 真机录制发现 SimpleGrassWind 缺少 AdditionalWPO 导致默认材质，已补必填连接和 Nanite/实例材质 usage，后续游戏日志不再出现这两项材质失败。录制 A-First、A-MaterialFix、A-GroundFix 均为失败/修正过程证据，不作验收通过。
- 真实地面偏差定位为 FBX 坐标转换及 Nanite Auto fallback 简化。地面源高度改为最终 UE 坐标；显式启用完整 fallback 碰撞。surface-audit.json 的 7 个采样点与高度函数一致，包括 (600,0)=46.457954 cm。植被/岩石/立杆与地面共用 Scripts/mounted_reference_layout.py。
- B-First 已在游戏中运行独立人马 Montage、6 个 Notify 阶段、1 次 Notify State 武器窗口、可见斧刃 Sweep，动作后恢复自然 AI。首招实际位移约 1948.7 cm；不是 C 普通输入。原速完整文件在 Docs/Verification/MountedChargeSample/ReferenceProduction/B-First/。18.03695 秒实际采集，425 帧；原音频18.02667秒，音视频时长门槛通过。有效采集23.56fps不代表UE帧率。
- 新配置为 5.8 秒，Launch .9、Strike 1.48、Pass1.98、ContactEnd2.18、Brake3.5、Recover4.3、Commit.72；这些是可见原片的制作判断，约0.2秒精度，遮挡处不是动作提取。位移约1951.875cm来自原生CurveFloat，由同一动作时钟驱动。旧3.55秒源动画和提交仍保留。
- B-First 马蹄支持期滑动约135cm/s，明显不足。已将接触曲线改为局部周期地面高度门槛；后续检测约92–93cm/s，仍需动态修整。早期配对时钟日志在马延迟求解前采样，已改为最终求解后记录；末端自动淡出导致提前跳到终点也改为由战斗生命周期结束 Montage。
- 新骑士死亡序列、独立马匹死亡 IK Retarget 候选及配对死亡 Montage 已导入。马的死亡源复用原 Quaternius Death，未另买资产。骑士死亡时释放原生脚蹬 IK。必须实际看死亡及重试，不能据此宣称缺口关闭。
- 回归 20260912-154418-13e090a3：sample_cleanup、sample_pre_cancel、retry 通过；hit_charge/dodge_charge 命中与闪避正确，但旧6秒测试提前结束及3.55秒固定断言使完整动作检查失败。已保留失败并修改测试等待/预期，待重跑。未删除旧测试。
- UE运行帧统计和录制分开：B-First整个运行会话含启动、采集，mean17.68ms、p50 8.53ms、p95 47.84ms，不能称稳定60fps，更不能用于1080p结论。C需外部窗口录制及真人正式输入。
- 环境编辑中一次早退崩溃及Blender源属性崩溃均已保存记录；只向明确归属于本轮已退出UE PID的孤儿报告进程发正常结束信号，无当前工程游戏驻留。后续编辑采用延迟正常关闭。
- 本轮所有 db1ee62 之后的修改仍需新提交/推送；main未合并。A/B仍为制作候选，C初测和最终验收均未完成。

### 16:04 更新

- 延长旧测试后发现实际游戏末尾停在5.799950秒，确为游戏边界错误；只在原生Montage已停止且距末尾小于0.0001秒时规范为真实终点，未提前跳过收势。新回归20260912-155941-67c79d45命中/闪避、完整恢复、6阶段和关闭判定均通过，保留两轮失败记录。
- 新动作源增加Blender原生四连杆IK落蹄约束，位置与同一根位移曲线匹配，导出后仍由UE Foot Placement/Leg IK处理实际坡地。离线支持期滑动从133.09降至0.84cm/s；这是离线结果，必须检查新游戏结果，不能直接视为实机通过。
- 1080p B-Revision1080试调曝光过亮且仍含旧末尾卡住问题，该录像保留为失败过程证据。当前已修亮度并等待引擎材质编译完成再启动A/B录像；未对录像变速或插帧。
- 增加uphill/downhill/cross/rock_edge显式QA场景参数；只用于自动功能检查，真人入口不传这些参数。

### 16:14 接触候选诊断

- B-NativeContact实机候选滑动约426.5cm/s，比旧版更差，已拒收；不能用离线0.84cm/s宣称修复。无UE脚锁对照仍约148cm/s。逐骨审计确认导入X/Z位置基本一致，Y为预期坐标镜像，并非FBX骨架损坏。
- 改用覆盖真实低位马蹄的接触阈值、抬蹄净空和Blender原生IK；离线支持期161.59→15.94cm/s。新AuthoredFootLock曲线只在此招关闭UE重复的水平锁定，保留原生Foot Placement坡面对齐；其他基础步态保持原脚锁。正在导入和实测，尚未验收。

### 16:38 回归修正

- 全六招首轮16项中13项通过；身体实际穿透为0，但倾斜盒体的旧水平距离估算为-3.22cm，导致误报。改为原生碰撞形状到胶囊轴的距离后body复测通过。
- 重劈确实漏判：旧兼容输入先经旧骨架限幅、再经新原生IK，且UE正Pitch使收势向后。改为原生IK接收原始握柄目标，重劈改为正确前倾；20260912-163441-abea0ea8/hit_overhead重新造成34伤害且只命中一次。需复测其他受影响动作，不沿用旧结果。
- 普通启动、E入战和提示点也发现固定Z假设，已一起改为真实地面查询。它们属于真实输入路径，不能仅修QA位置。
- 正在试验从巨大薄地面网格排除低精度距离场遮蔽，保留三角碰撞及正常阴影；目的是诊断草丛/肩臂的异常暗遮蔽。参考Epic Mesh Distance Fields说明，仍需看游戏对照。

- 接触审计进一步区分了踝点固定与蹄掌旋转：只锁踝点时，脚掌仍随父关节转动摩擦地面。新源动画增加原生Copy Rotation蹄掌方向约束，正在实机验证。旧较差候选仍保留，不作为已完成结果。
- 本轮只使用105个Meshy积分。未消耗永久积分、未买外部资产、未扩大到第二招制作。Poly Haven草木/地表均为CC0下载，其许可与校验清单随源文件归档。

### 16:55 更新

- 首批三件检查后，当前游戏远景仍是空山坡，缺少原片可见的城堡轮廓；据此补一件 Meshy 远景遗迹，任务 01a094d0-20af-77f7-816e-5fc07cb79343，实际35积分。本轮累计140，余额2224=824月度+1400永久，未超过首批200。不可见的城堡背面属于保守推断，不称精确复刻。
- B-SoleContact 新蹄掌 Copy Rotation 候选在完整冲锋支持期测得31.45cm/s，较前一实机约226改善，但仍有余滑，未验收。6阶段、1武器窗口、动作后恢复AI正常。1080p采集207帧/18.024s，约11.48fps，音轨18.005s；采集不足需外部录屏核查，不能称60fps。
- 环境照明增加免费CC0 overcast_soil_puresky HDRI候选，用原生Skylight漫反射照明，原天空仍用UE设施；下载校验与许可清单归档。等待实际查看亮度，不依靠截图曝光后处理。
